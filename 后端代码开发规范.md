# Memora 后端代码开发规范

> 版本：v1.0  
> 适用范围：Memora 后端 API、Worker、Migration 与公共基础设施  
> Go 版本：1.25.0  
> 架构：Gin MVC 模块化单体，Controller → Service → Repository

## 1. 规范目的

本文是团队后端开发、代码评审和交付检查的统一依据。开发人员必须同时遵守根目录的需求规格、API 接口文档和数据库设计文档；当文档发生冲突时，先停止实现并由负责人确认，不得自行扩展业务语义。

核心原则：

- 业务边界清晰，禁止跨层调用；
- 用户身份和数据归属必须由服务端校验；
- 长耗时任务必须进入 Worker；
- 跨模块协作必须复用 `internal/contracts`；
- 数据结构变更必须使用显式 SQL Migration；
- 外部调用必须有超时、取消、重试和大小限制；
- 不得把未运行的代码描述为已经通过验证。

## 2. 技术栈与固定版本

- Go：1.25.0；
- Web：Gin；
- ORM：GORM，复杂查询使用原生参数化 SQL；
- 数据库：PostgreSQL 17 + pgvector；
- 缓存与控制状态：Redis 7；
- 对象存储：MinIO；
- 配置：Viper；
- 日志：Zap + Lumberjack；
- 身份认证：JWT v5 + Argon2id；
- 数据迁移：golang-migrate；
- API：REST + JSON，Agent 事件使用 SSE。

禁止引入 MySQL、AutoMigrate、bcrypt 或第二套 Web/配置/日志框架。新增依赖前必须说明用途、维护状态、许可证、替代方案和对镜像体积的影响。

## 3. 目录职责

```text
cmd/server/                         API 进程入口
cmd/worker/                         Worker 进程入口
cmd/migrate/                        Migration 与管理员维护命令
configs/                            YAML 配置示例
deploy/                             Docker Compose 和部署资源
docs/                               架构、开发和 API 补充说明
internal/app/                       依赖组装、生命周期和优雅关闭
internal/api/v1/<resource>/         Controller 与路由
internal/middleware/                HTTP 中间件
internal/model/entity/              数据库实体
internal/model/dto/request/         HTTP 请求 DTO
internal/model/dto/response/        HTTP 响应 DTO
internal/repository/                数据访问接口与实现
internal/service/                   业务接口与实现
internal/contracts/                 跨模块 contracts v0.1
internal/worker/                    Worker 注册、执行、幂等和心跳
pkg/                                无 Memora 业务语义的基础设施封装
scripts/migrations/                 版本化 SQL Migration
```

目录红线：

- `cmd` 只负责启动、信号处理和退出码，不写业务逻辑；
- `internal/app` 只负责组装依赖，不实现业务规则；
- `pkg` 不得依赖 `internal`，不得保存 Memora 业务状态；
- `contracts` 不得引用 Gin、GORM Entity 或具体供应商 SDK；
- 业务模块不得创建重复的通用工具包。

## 4. 分层依赖规则

### 4.1 Controller

Controller 负责：

- 路由和 HTTP 状态码；
- Path、Query、Header 和 JSON Binding；
- 调用 Service；
- 使用 `pkg/response` 返回统一响应；
- 写入必要的成功审计事件。

Controller 禁止：

- 直接访问 GORM、Redis、MinIO 或模型 SDK；
- 编写 SQL、事务和业务状态机；
- 信任请求体中的 `user_id`、`knowledge_base_id` 授权信息；
- 向客户端返回内部错误、堆栈、SQL、Secret 或 Prompt。

### 4.2 Service

Service 负责业务用例、权限规则、状态机和短事务编排。Service 方法第一个参数必须是 `context.Context`，不得接收 `*gin.Context`。

Service 只能依赖 Repository 接口、跨模块 contracts、Entity 和必要的基础设施接口。跨多个 Repository 的事务由 Service 确定边界，但事务中禁止执行模型、MCP、MinIO、网页请求等长时间外部 I/O。

### 4.3 Repository

Repository 只处理数据访问和持久化错误映射。查询业务资源必须同时包含：

```text
resource_id
+ current_user_id
+ knowledge_base_id（知识库资源）
+ status / deleted_at 条件
```

禁止先按 ID 查询，再由上层补做用户归属校验。复杂向量检索、全文检索、批量写入、行锁和 `SKIP LOCKED` 使用原生参数化 SQL。

### 4.4 Model 与 DTO

- Entity 只描述持久化数据，不作为 HTTP Request/Response；
- Request DTO 只包含客户端允许提交的字段；
- Response DTO 不得暴露密码 Hash、密钥密文、内部错误和软删除字段；
- JSON 字段统一使用 `snake_case`；
- ID 对外使用 UUID 字符串；
- 时间对外使用 RFC3339，数据库使用 `timestamptz`，应用统一使用 UTC。

## 5. 新功能标准开发顺序

1. 对齐需求规格、API 契约和数据库设计；
2. 确认是否涉及 `contracts v0.1` 变更；
3. 添加或扩展显式 Migration；
4. 定义 Entity；
5. 定义 Request/Response DTO；
6. 定义 Repository 接口并实现所有权过滤；
7. 定义 Service 接口并实现业务用例；
8. 创建 Controller 和 Routes；
9. 在 `internal/app` 注入依赖并挂载路由；
10. 长耗时任务注册 Worker Source 与 Handler；
11. 同步 API、数据库、架构和配置文档；
12. 按本文交付清单自查。

不得先在 Controller 中临时实现业务，再“后续重构”。

## 6. Go 代码规范

### 6.1 命名与文件

- 包名使用小写单词，不使用复数堆叠和无意义缩写；
- 文件名使用 `snake_case.go`；
- 导出类型和方法使用清晰业务名称；
- 接口按能力命名，不添加 `I` 前缀；
- 构造函数使用 `New<Type>`；
- 布尔值使用 `Is`、`Has`、`Can`、`Enabled` 等可判断名称；
- 常量代替散落的状态字符串和错误码。

### 6.2 函数与接口

- 函数保持单一职责，参数过多时使用明确的参数结构体；
- 需要取消、超时或链路传递的方法必须接收 `context.Context`；
- 接口由使用方定义，保持最小能力集合；
- 禁止使用 `any` 逃避稳定业务结构，JSON 扩展数据除外；
- 禁止隐式 `init()` 注册和可变全局业务状态；
- Goroutine 必须有退出条件、并发上限和所有权说明。

### 6.3 错误处理

- 禁止忽略有业务意义的错误；
- 包装错误使用 `%w`，判断使用 `errors.Is/As`；
- Repository 返回可识别的数据错误，Service 映射为稳定业务错误码；
- Controller 统一交给 `pkg/response` 输出；
- 公开错误信息不得包含内部实现细节；
- `panic` 仅由 Recovery 兜底，不得用于正常业务分支。

### 6.4 格式

- 所有 Go 代码必须经过 `gofmt`；
- Import 由标准工具分组，禁止手工制造别名；
- 注释解释设计原因和边界，不复述代码；
- 禁止提交大段注释掉的旧代码、调试输出和临时 Secret。

## 7. HTTP 与 API 规范

- 业务前缀固定为 `/api/v1`；
- 登录以外的业务接口默认要求 Bearer Token；
- Middleware 顺序保持 Request ID、Trace、Metrics、AccessLog、Recovery、CORS、Auth；
- 所有响应使用统一 Envelope：`code`、`message`、`data/details`、`request_id`；
- 响应头返回 `X-Request-ID` 和 `X-Trace-ID`；
- 支持传入 W3C `traceparent`，不得信任其业务身份信息；
- 分页默认 page=1、page_size=20，page_size 最大 100；
- 列表查询必须有确定排序；
- 文件大小、请求体、工具结果和模型输出必须有上限；
- SSE 事件必须使用 `AgentEvent`，同一 Run 的 Sequence 单调递增；
- SSE 断线重连使用 RunID 和 Sequence 恢复展示，不输出完整内部思维链。

错误码必须稳定，禁止把错误文本当作客户端判断依据。新增错误码先更新 API 文档和 `internal/contracts/errors.go`。

## 8. 身份、权限与数据隔离

- 用户身份只能从 Auth Middleware 注入；
- JWT 仅接受 HS256，签名方法必须白名单校验；
- release 模式 JWT Secret 不少于 32 字符，禁止示例值；
- 密码使用 Argon2id，禁止保存或记录明文；
- 修改密码必须校验旧密码，新密码不得与旧密码相同；
- Redis 撤销令牌 TTL 等于 JWT 剩余有效期；
- 用户查询必须过滤 `status='active' AND deleted_at IS NULL`；
- 管理员初始化只创建不存在的用户，不覆盖现有凭证；
- 管理员密码重置必须使用显式维护命令；
- API Key、MCP Header 和认证信息必须加密存储，响应只返回脱敏值；
- 日志禁止记录 Password、Token、Authorization Header、Cookie、API Key、MCP Credential 和完整 Prompt。

## 9. 数据库与 Migration 规范

### 9.1 基本规则

- 禁止 AutoMigrate；
- Migration 位于 `scripts/migrations`，使用连续递增编号和 up/down 文件；
- 已共享 Migration 禁止回写，变更必须增加新编号；
- 表名和字段名使用 `snake_case`；
- 主键使用 `uuid DEFAULT gen_random_uuid()`；
- 时间字段使用 `timestamptz`；
- 软删除资源使用 `deleted_at` 并建立部分索引；
- 外键、唯一约束、CHECK 和业务索引必须在 Migration 中显式声明；
- P0 Schema 固定为数据库设计文档中的 20 张表。

### 9.2 事务规则

- 创建知识库时，知识库、默认目录、搜索配置和 Agent 配置在同一短事务中创建；
- 提交问题时，用户消息和 queued AgentRun 在同一短事务中创建；
- Agent 完成时，Assistant Message 和 AgentRun 在短事务中更新；
- 新索引构建完成后，再以短事务切换 `active_index_version`；
- 禁止在事务中等待 Worker、模型、网络、MCP 或对象存储。

### 9.3 检索强制条件

RetrievalService 至少过滤：

```text
current user_id
current knowledge_base_id
documents.deleted_at IS NULL
documents.processing_status = succeeded
chunk/vector.index_version = documents.active_index_version
document_vectors.status = ready
```

MemoryRetriever 至少过滤当前 UserID、active 状态和合法 Scope。Memory 不能作为知识事实 Citation。

## 10. Worker 开发规范

长耗时的文档解析、清洗、分段、向量化、重建索引、AgentRun、MemoryExtractor 和 MCP 后台工作必须由 Worker 执行。

每种任务需要注册：

- 唯一 Job Type；
- 业务状态表对应的 Source；
- 一个 Handler；
- 稳定的 Idempotency Key；
- Timeout、MaxAttempts 和失败状态落库规则。

Worker Source 复用 `import_tasks`、`agent_runs` 等业务状态表，不新增第 21 张通用任务表。领取任务时应使用数据库锁或原子状态更新，防止多 Worker 重复领取。

Handler 必须：

- 响应 Context 取消；
- 支持重复执行或由幂等键保护；
- 不无限重试；
- 不吞掉失败原因；
- 不在内存中保存唯一业务事实；
- 在成功、失败、超时和取消时更新业务状态；
- 对模型、MinIO、HTTP 和 MCP 调用设置独立超时。

## 11. contracts v0.1 规范

跨模块必须复用 `internal/contracts` 中的：

- RetrievalRequest、RetrievalResult、Citation；
- ConversationContext、AgentContext；
- MemoryQuery、MemoryQueryResult；
- RouterDecision、ExecutionMode；
- ToolContext、ToolCall、ToolResult；
- Plan、PlanStep、ReviewerResult；
- AgentEvent；
- ModelFactory、ChatModel、EmbeddingModel、Reranker；
- 稳定错误码和默认配置。

禁止不同模块分别定义同义结构。Contracts 变更必须说明：

1. 变更原因；
2. 受影响模块；
3. 是否向后兼容；
4. 数据和 API 迁移方式；
5. 文档同步内容。

破坏性变更必须经过团队共同评审。

## 12. RAG、Agent、Memory 与 MCP 专项规范

- ReAct 和 Plan-Execute 共用 RetrievalService、ToolExecutor 和 ModelFactory；
- KnowledgeSearchTool 只做工具适配，不重复实现检索算法；
- Router 非法输出由运行层兜底为 `react`；
- PlanStep 的工具提示只能引用 ToolRegistry 已注册名称；
- Citation 必须能追溯真实文档、Chunk 或网络 URL；
- 禁止模型生成不可验证的伪引用；
- 不保存完整 Chain-of-Thought，只保存必要的执行摘要、状态和审计信息；
- MCP P0 仅允许 `streamable_http` 和只读工具；
- MCP 工具调用必须同时满足知识库、Agent、Server、Tool 和授权关系均启用；
- URL 导入和 MCP 请求必须防止 SSRF，限制协议、地址、重定向、响应大小和超时；
- ToolResult 必须截断超限结果，并保存截断标记。

## 13. 配置与 Secret 规范

- 配置使用 `MEMORA_` 前缀环境变量覆盖 YAML；
- `.env.example` 只提供示例，不得保存真实 Secret；
- 本地启动复制为 `.env`，`.env` 禁止提交；
- Server/Worker 使用完整配置校验，Migration 只校验数据库配置；
- 生产环境禁止示例 JWT Secret、管理员密码和 MinIO 凭证；
- 配置错误必须在进程启动阶段失败，禁止运行中静默使用危险默认值；
- 不在代码、Migration、Dockerfile 或日志中硬编码生产密钥。

## 14. 可观测性规范

### 14.1 日志

- 使用结构化 Zap 日志；
- HTTP 日志至少包含 Request ID、Trace ID、Method、Path、Status、Duration 和 Client IP；
- Worker 日志至少包含 Job ID、Job Type、Attempt 和结果；
- 错误日志保留错误链，但对外响应隐藏内部原因。

### 14.2 审计

登录、退出、密码修改、用户资料修改，以及后续模型配置、MCP 配置、工具授权和敏感数据操作必须写审计事件。审计字段至少包含 Action、ActorID、Resource、Outcome、RequestID 和 TraceID。

### 14.3 指标与健康检查

- `/health/live`：进程存活；
- `/health/ready`：PostgreSQL、Redis、MinIO 就绪；
- `/health/workers`：Redis TTL 心跳中的存活 Worker；
- `/metrics`：Prometheus 文本格式 HTTP 与 Worker 指标。

禁止把外部模型或 MCP 的短暂故障直接定义为 API 进程不存活。

## 15. 并发、超时与可靠性

- 所有外部 I/O 必须继承 Context，并设置小于业务总超时的独立超时；
- 并发数必须由配置控制，禁止请求级无限创建 Goroutine；
- Goroutine 启动方负责等待或取消；
- 重试仅用于可重试错误，采用有限次数和退避；
- 数据库唯一冲突、业务状态冲突不得盲目重试；
- 幂等操作必须定义稳定键和有效期；
- 进程收到退出信号后停止领取新任务，取消运行中 Context，并在超时内关闭连接；
- Redis 不作为核心业务事实的唯一存储。

## 16. 文档与变更同步

以下变更必须同步文档：

- 新增或修改 API：更新根目录 API 文档及 `docs/API.md`；
- 新增或修改表字段：更新数据库设计文档并追加 Migration；
- 修改目录或依赖方向：更新 `docs/ARCHITECTURE.md`；
- 修改配置：更新 `.env.example`、YAML 示例、Compose 和 README；
- 修改 contracts：提交影响说明；
- 修改启动命令：更新 README 和 Makefile。

禁止只更新代码而让需求、API、数据库和实现相互矛盾。

## 17. 当前质量与交付约束

当前阶段按负责人要求不保留测试文件，因此：

- 不得声称功能已经通过自动化测试；
- 不得声称 Migration 已在真实数据库执行成功；
- 不得声称 Docker Compose 或服务运行态已经验收；
- 交付说明必须明确列出实际运行过的命令和未验证内容；
- 静态格式化和文本检查不能替代编译、测试与运行态验收。

如果后续恢复测试策略，至少覆盖 Service 业务规则、Repository 所有权过滤、API 契约、Migration 升降级、Worker 幂等与状态机。

## 18. 禁止事项

- 禁止 AutoMigrate；
- 禁止跨层直接访问；
- 禁止 Controller 写 SQL；
- 禁止 Service 依赖 Gin；
- 禁止按资源 ID 查询却遗漏 UserID/KnowledgeBaseID；
- 禁止明文保存或记录 Secret；
- 禁止无超时外部调用；
- 禁止无限重试和无限并发；
- 禁止在 HTTP 请求中同步执行文档索引或 Agent 长任务；
- 禁止修改已共享 Migration；
- 禁止复制跨模块 Contract；
- 禁止保存完整内部思维链；
- 禁止启用 MCP 写工具；
- 禁止把示例凭证用于生产环境；
- 禁止提交 `.env`、日志、构建产物和本地 IDE 配置。

## 19. 提交前检查清单

- [ ] 已对齐需求、API、数据库和 contracts；
- [ ] 代码符合 Controller → Service → Repository 依赖方向；
- [ ] 身份来自 Auth Middleware；
- [ ] Repository 包含用户归属、状态和软删除过滤；
- [ ] 外部调用有 Context、超时和大小限制；
- [ ] 长任务进入 Worker，并定义幂等、重试、取消和失败状态；
- [ ] 数据变更使用新的显式 Migration；
- [ ] JSON 使用 snake_case，时间和 UUID 格式正确；
- [ ] 错误响应未暴露内部信息；
- [ ] 日志未包含密码、Token、密钥、完整 Prompt 或思维链；
- [ ] 关键敏感操作写入审计事件；
- [ ] 指标和健康状态已考虑；
- [ ] 配置示例和开发文档已同步；
- [ ] Go 版本保持 1.25.0，代码已执行 `gofmt`；
- [ ] 交付说明如实列出已执行和未执行的验证。

