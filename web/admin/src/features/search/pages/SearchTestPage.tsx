import { PendingFeaturePage } from '@/features/shared/PendingFeaturePage';
export function SearchTestPage() {
  return <PendingFeaturePage title="检索测试" description="验证关键词、向量、RRF 和 Reranker 结果。" capability="search" />;
}
