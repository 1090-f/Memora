import { PendingFeaturePage } from '@/features/shared/PendingFeaturePage';
export function McpPage() {
  return <PendingFeaturePage title="MCP 工具" description="配置服务、发现工具并管理只读授权。" capability="mcp" />;
}
